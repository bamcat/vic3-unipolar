widget = {
	name = io_main_panel_structure
	layer = layer_ingame_menu
	size = { 100% 100% }
	visible = "[GetMetaPlayer.GetPlayedOrObservedCountry.IsValid]"
	alwaystransparent = yes
	using = hud_visibility
	
	io_panel = {}
}
